# Spam-detection-in-ML
This project is basically defines a model which detect the email in your system and predict whether it is spam or real. This model can be upgraded by fitting more data into it . #Python #ML 
